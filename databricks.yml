# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 3ac77eef-49ae-4841-9305-6f00eaccdcd6

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: http://adb-7405610553163346.6.azuredatabricks.net
      root_path: /Workspace/Users/debendrabarik083_gmail.com#ext#@debendrabarik083gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksazureproject
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://adb-7405610553163346.6.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/debendrabarik083_gmail.com#ext#@debendrabarik083gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/debendrabarik083_gmail.com#ext#@debendrabarik083gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksazureproject
      schema: prod
    permissions:
      - user_name: debendrabarik083_gmail.com#ext#@debendrabarik083gmail.onmicrosoft.com
        level: CAN_MANAGE
